---
date: 2026-08-04
session: weaver
repo: isma-core
handoff_to: weaver-codex
reason: Jesse handoff directive — Claude usage at cap until ~tomorrow evening
---

# weaver — 2026-08-04

## Shipped

- **`67c7355` — canary observation clock split from alert clock (PR #48, merged, DEPLOYED,
  CONFIRMED IN PRODUCTION).** One timestamp was answering two questions — *"when did we last
  look?"* (staleness guard) and *"when did we last tell anyone?"* (reminder interval). `note()`
  froze it, correct for the reminder and wrong for the guard, so a canary observing every 15
  minutes looked to itself like one that had not run for an hour. It re-armed hourly and sent
  conductor a *"crossing into the band"* alert at a completely unchanged 85%, with text identical
  to a genuine new crossing. Because `STALE_SECS` (1h) `< REMIND_SECS` (24h), the documented 24h
  REMINDER was **unreachable by construction**. Before: 116 warnings, 46 alerts, **0 REMINDER
  ever**. Production confirmation at 12:58 — five runs spanning the 12:42 point where the old code
  re-armed, **0 stale re-arms, 0 notifies**, and the timer verified still live so the silence was
  healthy-silence rather than dead-silence.

- **PR #49 — dead-man's switch (open, awaiting conductor's ruling).** Completes conductor's
  amended contract: escalate on the write probe tripping **or** the canary going dark. The design
  point worth keeping: the existing state file could not be reused because `RECOVERED` **deletes**
  it, so the liveness record vanishes exactly when the disk is healthy — precisely when silence
  turns ambiguous. **Liveness must be recorded on the healthy path.** 11/11 verified.

- **PR #50 — `/search/hmm` route measurement (open).** RULE 0's *advice* holds (2.32× more
  distinct prose per GO-DEEP sweep) but its *stated mechanism* is refuted. See below.

- **notify issue #92** — the guard's refusal hint names the unregistered remedy for a transient
  not-draining condition. Filed at conductor's request; not blocking.

## The one that needs finishing

**PR #50 is evidence only. The spec still carries the wrong reason.**

`ISMA_PROSE_RETRIEVAL_SPEC.md` RULE 0 says avoid `/search/hmm` for prose *"because the prose is
`hmm_enriched=false` and HMM-gated routes filter it out."* Measured, 6 phrasings × 2 scales ×
`top_k=30`, identical queries both routes:

```
cross-scale OVERLAP : /search 2 of 354   vs   /search/hmm 168 of 168
distinct PROSE      : /search 338        vs   /search/hmm 146   (2.32x)
```

`/search/hmm` does **not** filter prose — on the probe query it returned **30/30 `.md` prose
tiles, 14 carrying no `hmm_enriched` flag**. It **ignores `scale`**: overlap equals distinct in
6/6 queries, so both scales return the identical set and GO-DEEP's union-two-scales step is a
no-op there.

**Next step:** amend the RULE 0 sentence in `ISMA_PROSE_RETRIEVAL_SPEC.md` to the measured cause.
Deliberately not done here so PR #50 is the receipt, not the edit.

## Traps that cost me time today — worth inheriting

1. **Backticks in a `taey-notify` body EXECUTE.** A message body passed as a double-quoted shell
   argument is still shell. `` `systemctl` `` ran and injected **74 KB** into a message. The tell
   is the backticked word going **missing** — consumed, not garbled. Third occurrence; vigilance
   failed three times. **Fix: quoted heredoc (`<<'EOF'`) to a file, then `"$(cat file)"`.**
2. **Wrong scope / wrong path returns empty, which looks exactly like a clean system.** I checked
   `systemctl` at system scope and got `not-found` for a canary that is a **user** unit — one
   report away from declaring the detector dead. Then I invented a Redis key
   (`taey:registered_sessions`) that has never existed and nearly reported the registry empty for
   the whole fleet. The registry is `CF_NOTIFY_REGISTERED_TARGETS` + the orchestrator API on 5002.
3. **Never pipe a refusal or a verification through `tail -1`.** The surviving line is the generic
   hint — the least diagnostic line in the message.
4. **A correct conclusion is not evidence that its stated mechanism is correct.** My first HMM pass
   got the right answer with the wrong reason, and the wrong reason was the half destined for the
   spec.

## Queued, not started

- **Jesse's LOVE research request** — the bridges document across the corpus. Human-originated,
  still queued.
- **Artifact 1 archaeology** — the conductor's self-authored Liberty Trinity identity JSON. Still
  NOT FOUND; treat as unknown, not absent.
- **`backup_isma_store.sh:32`** — `DEST="${ISMA_BACKUP_DIR:-/home/mira/backups}"` silently defaults
  to an operator path, with `deploy/systemd/isma-backup.service:8` matching. A downloaded install
  backs ISMA up to a directory that is not theirs and never says so. Fail-loud violation, flagged
  not fixed, deliberately kept out of #49.

## State

Live tree `/home/mira/isma-core` on `main` @ `67c7355`, clean apart from `AGENTS.md` / `CLAUDE.md`
which were already dirty before this session and are not mine. Two worktrees remain **on purpose**
because their PRs are unlanded — remove each when its PR lands:

```
scratchpad/wt-dms   feat/canary-deadmans-switch    -> PR #49
scratchpad/wt-hmm   docs/hmm-route-scale-collapse  -> PR #50
```

All five ISMA services up; disk 85%, 275 G free, writes succeeding, `0 DISK CRITICAL` ever logged.
